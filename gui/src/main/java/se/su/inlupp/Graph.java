/*package se.su.inlupp;

import java.util.*;

public interface Graph {

    void add(City city);

    void remove(City city);

    void connect(City from, City to, String name, int weight);

    void disconnect(City from, City to);

    void setConnectionWeight(City from, City to, int weight);

    Set<City> getNodes();

    Collection<Edge> getEdgesFrom(City city);

    Edge getEdgeBetween(City from, City to);

    boolean pathExists(City from, City to);

    Collection<Edge> getPath(City from, City to);

    Collection<City> getDFSPath(City from, City to);

    Collection<Edge> getShortestPath(City from, City to);

    Collection<Edge> getLeastCostlyPath(City from, City to);
}

*/